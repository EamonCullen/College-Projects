1.) Cons function takes 2 arguments and returns a pair

2.) > (cons '1 (cons '2 (cons '3 '())))
'(1 2 3) (cons '1 (cons '2 '3)=(cons '1 '(2 3))= '(1 2 3)(first 3 and empty list forms pair then 2 and 3 forms a pair and 1 & (2 3) forms pair so result is '(1 2 3)

3.) List function always returns a list constructed from parameters instead of forming pair as in cons

4.) Append function always takes lists as parameters and returns the concatenation of different lists